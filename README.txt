An implementation of the PageRank Algorithm in three different ways: Iterative, Algebraic and using the Power Method. The data is read from a file as a graph and stored inside an adjancency matrix, then the array of page ranks is calculated.

